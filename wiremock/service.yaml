apiVersion: v1
kind: Service
metadata:
  name: wiremock-service
spec:
  selector:
    app: wiremock
  ports:
  - port: 80  # External port
    targetPort: 8080  # WireMock container port
  type: LoadBalancer  # Makes the service externally accessible
